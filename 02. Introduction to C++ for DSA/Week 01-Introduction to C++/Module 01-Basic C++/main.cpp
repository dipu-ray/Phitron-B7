#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    // int a, b;
    // cin >> a >> b;
    // if(a < b) {
    //     cout << a << endl;
    // }
    // else {
    //     cout << b << endl;
    // }
    // cout << min(a, b) << endl; // Min
    // cout << max(a, b) << endl; // Max

    // Find out a value of infinite values
    // cout << min({10, 20, 30, 40, 50}) << endl;
    // cout << max({10, 20, 30, 40, 50}) << endl;

    // Swap value with function
    int x, y;
    cin >> x >> y;
    swap(x, y);
    cout << x << " " << y << endl;
    return 0;
}