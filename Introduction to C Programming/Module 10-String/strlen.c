#include <stdio.h>
#include <string.h>

int main() {
    char s[100];
    scanf("%s", &s);

    // int size = strlen(s);
    // printf("%d", size);

    // In short
    printf("%d", strlen(s));

    return 0;
}