#include <stdio.h>

int main() {
    // char s[5] = {'D', 'I', 'P', 'U'}; // array[5] for null character in string.
    // char s[5] = "DIPU"; // It's also called string.
    char s[30] = "Dipu Ray Hriday"; // With space initialization
    printf("%s", s);

    return 0;
}