#include <bits/stdc++.h>
using namespace std;

int main() {
    // vector<int> v;
    // cout << v.capacity() << endl;

    // v.push_back(10);
    // cout << v.capacity() << endl;

    // // When we cross the capacity then value are double
    // v.push_back(10);
    // cout << v.capacity() << endl;

    // v.push_back(10);
    // cout << v.capacity() << endl;

    // v.push_back(10);
    // cout << v.capacity() << endl;

    // v.push_back(10);
    // cout << v.capacity() << endl;



    // Clear() function
    vector<int> v;

    v.push_back(10);
    v.push_back(10);
    v.push_back(10);

    cout << v.size() << endl;

    return 0;
}